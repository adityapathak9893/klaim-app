import { AppState, CompanyInfoData } from "../interfaces";

export const initializedCompanyInfoData: CompanyInfoData = { info: "" };

export const initializedAppState: AppState = {
  companyInfoData: initializedCompanyInfoData,
};
